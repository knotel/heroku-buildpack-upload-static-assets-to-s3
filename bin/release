#!/usr/bin/env bash
# bin/release <build-dir>

export BUILD_DIR=$1

SCRIPT_DEST=${BUILD_DIR}/_s3_upload

cat <<EOF
---
default_process_types:
  release: cd _s3_upload && yarn install && node upload.js
EOF
